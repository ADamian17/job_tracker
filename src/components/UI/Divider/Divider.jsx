import React from 'react';

import './Divider.scss';

const Divider = () => (
    <div className="divider">
        {/* NOTE non-breaking space */}
        &nbsp; 
    </div>
);

export default Divider;
